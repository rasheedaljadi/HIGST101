# تقرير تدقيق قدرات API وعمليات AliExpress الحية ومسار الدفع اليدوي — Procurement V2

## 1. ملخص الفحص وحالة الاتصال الحي (Live API & OAuth Status)

تم بنجاح تنفيذ الفحص التشغيلي المباشر لقدرات واجهة برمجة التطبيقات (API) وموصل **AliExpress Open Platform (IOP)** على الخادم (`76.13.79.242`) عند الـ SHA الموحد:

```
11eeeeb088f2cd5ef2ce3ac2cd9d5bcb4a5bec92
```

### نتيجة فحص الاتصال الحي ورمز الـ OAuth:
- **معرّف المتجر / البائع المرتبط (Seller ID):** `4586371333`
- **حالة رمز الدخول (OAuth Access Token):** ✅ **نشط وصالح (Active & Valid)** ومحفوظ بشكل مشفر في الخادم (`aliexpress_tokens`).
- **فحص الاستجابة الحية (Live Introspection Call):** تم تنفيذ استدعاء قراءة فحص حي (`aliexpress.ds.product.get`) وحصل على **`HTTP 200 OK`** من بوابة AliExpress الرسمية (`api-sg.aliexpress.com`) بنجاح تام وبدون أي أخطاء.

---

## 2. مصفوفة قدرات API الرسمية (API Capabilities Matrix)

| العملية المطلوبة | Method الفعلي المعتمد | Scope المطلوب | نوع العملية (Mode) | الحالة التشغيلية في هايست |
|---|---|---|---|---|
| **تبادل وتجديد الـ OAuth** | `system.oauth.token / refresh` | OAuth General | Write Token Store | ✅ **مفعل وصالح (HTTP 200)** |
| **استعلام بيانات وأسعار المنتجات** | `aliexpress.ds.product.get` | `aliexpress.ds.product` | Read Only | ✅ **مفعل وتم اختباره بنجاح** |
| **استعلام حالة وتتبع الشحنات** | `aliexpress.ds.order.get` | `aliexpress.ds.order` | Read Only | ✅ **مفعل ومقيد للأوامر النشطة** |
| **إنشاء طلب غير مدفوع (Place Order)** | `aliexpress.ds.order.create` | `aliexpress.ds.order` | External Write | ⏳ **مشروط بموافقة منفصلة لكل أمر** |
| **الدفع الآلي / السحب المباشر** | *N/A (محظور معمارياً)* | Payment | Financial Write | ⛔ **محجوب 100% (دفع يدوي في AliExpress)** |
| **إلغاء الطلب الخارجي** | `aliexpress.ds.order.cancel` | `aliexpress.ds.order` | External Write | ⛔ **محجوب حتى صدور أمر استثناء منفصل** |

---

## 3. ضوابط الأمان وعدم وجود آثار جانبية (Safety Invariants)

1. **الـ Idempotency والقفل:** كل طلب إنشاء خارجي يشتق مفتاح التطابق الصارم (Correlation Key) مباشرة من رقم `Supplier PO` المعتمد لمنع تكرار إنشاء أي طلب عند انقطاع الاتصال.
2. **عزل المخزون والحسابات:** استلام تحديثات حالة المورد (Order Status Polling) يحدث فقط حقول الحالة التاريخية واللوجستية، ولا ينشئ أي حركة مخزون أو Handoff أو قيد مالي من تلقاء نفسه.
3. **الدفع المالي اليدوي الحصري (Manual Payment):** لا يملك النظام أي صلاحية لسحب أموال أو دفع تلقائي؛ السداد يتم يدوياً داخل منصة AliExpress بواسطة الموظف المفوض، ثم يسجل إقرار الدفع في هايست.

---

## 4. نموذج طلب الموافقة لإنشاء أول أمر خارجي محدد (Pending Per-Order Approval)

قبل إرسال أي استدعاء `aliexpress.ds.order.create` لإنشاء أول أمر شراء حي غير مدفوع على AliExpress، يتطلب النظام موافقة صريحة من المسؤول في هذه المحادثة تتضمن التفاصيل التالية:

```text
========================================================================
طلب اعتماد إنشاء أمر شراء غير مدفوع على AliExpress (Unpaid Order Creation)
========================================================================
1. رقم أمر الشراء الداخلي (Supplier PO): SPO-LIVE-001 (أو المعرف المحدد)
2. المورد / متجر علي إكسبرس: [اسم/معرف متجر البائع المعتمد]
3. بنود الطلب وSKU: [SKU / Variant ID / Product URL]
4. الكمية المطلوبة: [الكميات للعجز الفعلي فقط]
5. عنوان محطة التجميع: [محطة توريد الرياض - السعودية hayest_dropship_sa]
6. العملة والتكلفة التقديرية: [USD xx.xx]
7. الأثر المالي والتشغيلي: سيتم إنشاء طلب حقيقي غير مدفوع يظهر في لوحة AliExpress، 
   ويصبح مستحق السداد اليدوي من قبل المفوض المالي.
========================================================================
```

---

## 5. الحكم النهائي الموثق

```
LIVE ORDER CREATION AND STATUS SYNC ACTIVATED — MANUAL PAYMENT AWAITS PER-ORDER APPROVAL
```
